/**
 * Main server module.  The purpose of this module is to provide the
 * corresponding server side API implementation.  So for each
 * `/imports/api/COLLECTION.js` there would be a corresponding
 * `/imports/server/COLLECTION.js` at minimum the server collection
 * should ensure there is an index to the mongo collection.
 * @module /imports/server
 */
