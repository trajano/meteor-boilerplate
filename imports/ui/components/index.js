/**
 * UI components module.
 *
 * This contains the Angular components and other responsibilities that are
 * used on the application.  It is separated so the global configuration
 * semantics are in a separate module.
 *
 * @module imports/ui/components
 */
import module from './module'
import './meteorSample.component'
import './downgraded'

export default module.name
