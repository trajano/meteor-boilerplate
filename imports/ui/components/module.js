/**
 * UI Components Angular module definition.
 *
 * This uses the name `module.js` rather than `ui.components.module.js` as
 * each AngularJS module would have a `module.js` and index.js it would
 * allow for easier module renames.
 *
 * @memberof module:imports/ui/components
 */
export default angular.module('ui.components', [])
