/**
 * This is the entry point for the routes.  It primarily contains imports
 * to other top-level routes that are defined by the system.  In addition
 * this module defines global triggers and not found handling.
 * @module imports/routes
 */
import './global'
import './home'
import './notFound'
import './signIn'
